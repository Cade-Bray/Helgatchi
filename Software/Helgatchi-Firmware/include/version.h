#pragma once

// Hand-bumped strings shown on the Info screen. Update at release.
// FW_VERSION_STR, BUILD_DATE_STR and UI_VERSION_STR are generated each build
// by scripts/build_info.py — see build_info.h. FW_VERSION_STR comes from the
// v* git release tag; do not hardcode it here.

#define HW_REV_STR       "R2.9"
#define GAME_VERSION_STR "0.0"

#include "build_info.h"
